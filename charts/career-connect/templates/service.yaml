apiVersion: v1
kind: Service
metadata:
  name: {{ include "career-connect.fullname" . }}
  labels:
    {{- include "career-connect.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- with .Values.service.nodePort }}
      nodePort: {{ . }}
      {{- end }}
  selector:
    {{- include "career-connect.selectorLabels" . | nindent 4 }}
